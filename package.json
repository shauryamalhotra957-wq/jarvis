{
  "name": "jarvis",
  "version": "1.0.0",
  "description": "JARVIS: a futuristic assistant with a Three.js globe, satellite pointer, glowing arc-reactor taskbar orb, command intelligence, tests, and docs.",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "start": "vite --host 127.0.0.1 --port 4373",
    "dev": "vite --host 127.0.0.1 --port 4373",
    "build": "vite build",
    "desktop:serve": "node desktop/server.mjs",
    "desktop": "npm run build && npm run desktop:serve",
    "test": "node --test tests/*.test.js",
    "validate": "npm run test && npm run build"
  },
  "dependencies": {
    "@vitejs/plugin-basic-ssl": "^2.1.0",
    "vite": "^7.0.0",
    "three": "^0.178.0"
  },
  "devDependencies": {},
  "license": "UNLICENSED"
}
